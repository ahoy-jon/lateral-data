Lateral Data Desktop
----------------
A User Interface for Lateral Data.

Installation
----------------
* install Maven 3.x.x
* clone this repository
- run `mvn clean install`
- run `mvn exec:exec` (without clean !)

A systray should appear.